[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "housingmodel"
version = "1.0.0"

[tool.pytest.ini_options]
pythonpath = [".", "src"]
testpaths = [
    "tests",
    "integration",
]
